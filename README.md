# Chess

Simple chess game made with Python.

## Run

```bash
python chess_game.py
```

You can also double-click `Play Chess.bat` on Windows.

## Requirements

- Python 3
- `pygame`
- `python-chess`

Install the Python packages with:

```bash
pip install pygame python-chess
```
